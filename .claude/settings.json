{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|resume|clear",
        "hooks": [
          {
            "type": "command",
            "command": "echo PriorityKB session active. Before responding to the first user message, execute SESSION_PROTOCOL.md Opening Ritual Steps 1-7 in order.Do not generate code before Step 7 go. Reply in English per CLAUDE.md language convention (always English in operating sessions, see ADR-0007); no Hebrew in output."
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "node scripts/hook-gh-pr-create-precheck.mjs"
          }
        ]
      }
    ]
  }
}
